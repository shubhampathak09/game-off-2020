// 13

findmid(l)
{
	slow=l.get(0);
	fast=l.get(0);
	
	while(fast)
	
}

LinkedList<Integer> mergersort(LinkedList<Integer>ol,int start,int end
{
	
if(start==end)
{
	LinkedList<Integer>ans=new LinkedList<>();
	ans.add(l1.get(i));
	
	return ans;
}	


int mid=li.size()/2 ;

LinkedList<Integer>left=mergesort(li,0,mid);
...   right=mergersort(l1,mid+1,end);

return merge(left,right);	
	
	
}
